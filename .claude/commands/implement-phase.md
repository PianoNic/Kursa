Implement the specified phase from the roadmap in CLAUDE.md. 

Before starting:
1. Read `CLAUDE.md` to understand the full project context
2. Read `docs/features.md` for detailed feature descriptions  
3. Read `docs/architecture.md` for design decisions
4. Check what already exists in the codebase

For each task in the phase:
1. Plan the implementation (entities, services, endpoints, components needed)
2. Implement backend first (domain → application → infrastructure → API)
3. Implement frontend second (service → component → routing)
4. Write tests for critical paths
5. Verify with `dotnet build`, `ng build`, and `dotnet test`
6. Update the phase checklist in CLAUDE.md (mark completed items with [x])

Always commit after completing each logical unit of work with a descriptive commit message.

Phase to implement: $ARGUMENTS
